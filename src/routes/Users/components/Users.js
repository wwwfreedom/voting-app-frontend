import React, { PropTypes } from 'react'
import sty from './Users.scss'

export const Users = () => (
  <div className={sty.container}>
    <h4>Users</h4>
  </div>
)

Users.propTypes = {
  // : PropTypes.
}

export default Users
