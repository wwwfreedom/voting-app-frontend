import React, { PropTypes } from 'react'
import sty from './User.scss'

export const User = () => (
  <div className={sty.container}>
    <h4>User</h4>
  </div>
)

// User.propTypes = {
//   : PropTypes.
// }

export default User
